<article class="card border-box-sizing" style={Astro.props.style}>
	<slot />
</article>

<style is:global>
	@keyframes fadeIn
	{
		0%
		{
			transform: translateY(12px);
			opacity: 0;
		}

		100%
		{
			transform: translateY(0);
			opacity: 1;
		}
	}
	.card
	{
		overflow: hidden;
		transform-origin: center bottom;
		animation: fadeIn 450ms ease var(--delay, 0ms) 1 normal forwards;
		opacity: 0;
	}

</style>
