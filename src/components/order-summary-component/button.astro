<button class:list={[ "btn", Astro.props.class ]}><slot/></button>

<style is:global>
	.btn
	{
		font-weight: 700;
		background: transparent;
		display: inline-block;
		border: 0;
		padding: 0;
		cursor: pointer;

		transition: color 350ms, transform 175ms;
	}


	.btn--regular
	{
		color: var(--clr-neutral-900);
		border-radius: 10px;

		background-color: var(--clr-primary-400);
		padding: 0.75em 2.25em;
		transition: background-color 350ms, transform 175ms;

		box-shadow: 0px 10px 10px 0 var(--clr-primary-400a125);
	}

	.btn--regular:hover
	{
		background-color: var(--clr-primary-400a750);
	}


	.btn--ghost
	{
		min-width: fit-content;
		color: var(--clr-neutral-400);
		background-color: transparent;
	}

	.btn--ghost:hover
	{
		color: var(--clr-neutral-200);
	}


	.btn--link
	{
		min-width: fit-content;
		color: var(--clr-primary-400);
		text-decoration: 2px underline;
		text-underline-offset: 1px;
	}

	.btn--link:hover
	{
		color: var(--clr-primary-400a750);
		text-decoration: 0;
	}

	.btn:active
	{
		transform: scale(0.95);
	}
</style>
